import { createAction } from 'redux-actions'

export const loadNetwork = createAction('load network')
export const shareNetwork = createAction('share network')
export const clearNetwork = createAction('clear network')
